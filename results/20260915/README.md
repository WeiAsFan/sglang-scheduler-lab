# 2026-09-15 轻量证据批次

## 批次性质

本批次是对服务器上已有运行目录的轻量证据导出和同步，不是新的正式实验，也没有重新启动 SGLang 服务、`run_case.py` 或 GPU 轮次。导出工具只读取已有记录，没有改写原始运行目录。

证据来源为服务器工作区 `/mnt_d/huangxiaoyuan/sglang-scheduler-lab-v1.0` 下的 `runs/`。服务器上的导出目录为 `results/20260915/evidence/`，已同步到本机同名目录。

## 覆盖范围

- 覆盖 92 个一级运行目录。
- 共保留 36,658 条按 `rid` 合并的请求记录。
- 保留 910 条失败或缺失客户端记录；缺失记录没有被当作成功、零等待或零错误。
- 导出错误数为 0，详见 `export-errors.log`。
- `evidence-manifest.csv` 是本批次索引，记录每轮的来源、请求数、失败或缺失数、调度摘要和可选文件情况。

每个运行目录通常包含 `requests.jsonl`、`failed-requests.jsonl`、`scheduling-summary.json`、`scheduling-timeline.jsonl`、`log-excerpts.txt` 和 `export-info.json`，并在源目录存在时复制运行元数据、服务器信息、GPU 与环境信息。请求表只保留输入长度、时间、TTFT、输出／缓存 token、成功状态、错误和服务端首次入队／准入等轻量字段，不包含完整输入 token 数组。

普通性能运行没有导出回答正文。`20260914-text-fcfs` 和 `20260914-text-short-remaining` 是专门的正常文本观察，均为 3/3 成功，并按手册要求保留回答正文；这两轮只用于基本功能观察，不代表质量评测通过。

## 重点核对

aging 对照的轻量调度摘要显示：

- `aged-remaining`：有 `aged` 的候选扫描调用为 862/2059，占比 `0.4186498300145702`，最大 `aged` 数为 80。
- `aged-cost`：有 `aged` 的候选扫描调用为 861/2058，占比 `0.41836734693877553`，最大 `aged` 数为 79。

这些数字说明 300000 ms aging 对照中 aging 实际参与了调度，不能解释为纯成本排序或公平性保证。

失败轮次、启动失败目录、缺失客户端／服务端记录以及错误日志片段均按原状保留，没有用重试结果覆盖原始轮次。

## 文件与上传边界

本机仍保留完整的 `results/*/raw/*.tar.gz` 原始归档；完整 token 输入、完整日志和未压缩的 `runs/` 仍保留在服务器或本机归档位置。这些大文件已由 `.gitignore` 排除，不应通过 GitHub 网页上传。

本批次的轻量证据、清单、配置快照、导出脚本测试和本说明可提交到仓库。导出证据中的最大单文件约 415 KB，未发现超过 GitHub 网页 25 MB 限制的文件。
