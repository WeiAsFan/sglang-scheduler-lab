# SGLang 调度实验：共同语言

本词表统一项目定位、调度对象和实验指标的含义，用于简历、面试讲解、代码讨论和实验分析。

## 项目与交付

**目标岗位**：AI 推理系统工程师。本项目展示大模型推理部署、请求调度、性能测量和实验解释能力。

**面试项目**：以支持求职讲解和现场答辩为主要交付目标的小型工程项目。
_避免_：默认称为论文研究或生产系统。

**面试交付面**：项目到达面试官的方式，包括简历中的简短介绍、面试中的详细讲解和对追问的现场答辩。

**支持性仓库**：为面试叙事提供可核验代码、实验产物和文档的证据载体。
_避免_：把 GitHub 仓库页面等同于项目价值或默认展示界面。

**原生策略**：所讨论的 SGLang 版本已经提供的调度策略。
_避免_：把某一版本的策略清单称为所有版本的固定清单。

**候选策略**：本项目提出、尚需实现和实验评价的排序方法或原生策略变体。
_避免_：把候选方案称为已实现优化，把预期收益称为实测结果。

## 请求与调度

**请求排序**：决定等待请求被调度器考察的先后顺序。
_避免_：把排到队首等同于立即执行。

**批次准入**：结合 token 预算、KV 空间和当前批次状态，决定请求能否进入一次执行批次。

**prefill**：处理已知输入 token 并形成后续生成所需状态的计算阶段。

**decode**：在已有上下文状态上逐步生成后续 token 的计算阶段。

**前缀命中**：请求开头的一段 token 在允许共享的缓存范围内已有可复用状态；前缀长度以实际 token 序列匹配结果为准。
_避免_：把文本相似、预设共享比例或字符数称为实际前缀命中。

**可复用前缀 token 数**：当前可被请求复用、从而省去对应 prefill 计算的前缀长度；涉及多层缓存时还需区分状态所在层级。

**剩余 prefill token 数**：一次候选评估时，该请求为完成输入处理仍需计算的 token 数。
_避免_：把它叫作剩余生成长度或剩余请求总工作量。

**剩余 prefill 成本**：对剩余输入处理所需计算资源或执行时间的估计；token 数是其中一种代理量。
_避免_：把 token 数直接写成毫秒耗时。

**等待时间（首次排队等待）**：请求首次入队至首次获准进入 prefill 批次的时间；尚未准入时，指截至当前时刻已经等待的时间。
_避免_：混入传输时间、分块续算间隔或回退后的等待，或把准入时刻称为 GPU 真正开始计算的时刻。

**本轮排队时间**：请求最近一次进入等待队列后经过的时间；发生回退重新入队时开始新的一轮。

**token 等待量**：请求入队后，调度器累计处理的 prefill token 数增量；它度量工作进度上的等待。
_避免_：把它等同于实际经过的秒数。

**老化（aging）**：随着等待增加而提高请求调度优先级的方法。

**超时提升**：请求等待达到选定阈值后，被提升到普通候选请求之前考察的规则。
_避免_：称为请求超时取消，或宣称阈值就是最大等待保证。

**有界候选集合**：一次调度中接受额外缓存匹配和成本评估的、数量受限的请求集合。
_避免_：把限制候选数量等同于整个调度器只有常数开销。

**分块 prefill**：把同一个请求的输入处理分成多个执行片段的方式。

**饥饿**：请求持续被其他请求越过、长期得不到服务的现象。

## 实验与指标

**短请求／长请求**：按原始输入 token 长度、使用预先约定的分组边界划分的请求群体。
_避免_：根据各策略运行后产生的命中长度重新定义短请求。

**TTFT**：客户端发出请求到收到首个生成 token 的经过时间。
_避免_：把 TTFT 等同于纯 prefill GPU 时间或服务端排队时间。

**P95／P99 TTFT**：指定请求群体中 TTFT 分布的第 95／99 百分位数。

**长请求最大等待时间**：一次实验中长请求群体的首次排队等待最大观测值；尚未准入和未完成请求的等待状态需要另行呈现。
_避免_：称为策略提供的理论等待上界。

**吞吐**：单位时间完成的请求数或产生的 token 数，需明确统计对象、单位和观测时间窗口。

**前缀缓存命中率**：在约定观测范围内，可复用前缀 token 数占相应输入 token 数的比例。
_避免_：混同请求命中比例、单批次命中率和整轮实验的 token 加权命中率。

**调度 CPU 开销**：CPU 为候选选择、缓存匹配、成本计算和排序等调度工作付出的时间；线程执行时间与调用经过时间是不同口径。

**负载轨迹**：一组请求的输入、生成要求和计划到达时刻；它定义被比较策略面对的工作负载。

**消融实验**：保持其他条件一致，只改变一个策略组成部分，以解释该部分带来的收益和代价的实验。

**实验产物**：一次运行留下的输入副本、配置、请求级记录、服务日志、指标汇总和分析图表。

## 运行环境

**远程实验服务器**：执行真实 SGLang GPU 实验的服务器，通过 Linux 登录设备以 `ssh` 访问；可使用本地 Git，但不能访问外网或向 GitHub 推送。

**Linux 登录设备**：可访问外网、负责下载实验文件、登录服务器和接收实验产物的 Linux 设备；GitHub 发布通过网页端手动提交。

**本地工作区**：当前 Windows 环境中的代码、文档和结果分析目录。
_避免_：把本地静态分析或模拟结果称为远程服务器上的 GPU 验证。

**实验产物回传与发布**：产物从服务器传到 Linux 登录设备，再由操作者通过 GitHub 网页端上传并手动提交的过程。

**vLLM**：本项目背景中提及的另一种推理引擎；当前调度研究和主体实验对象是 SGLang。
