# 协作约定

## 沟通

- 对话、说明、计划、总结、提交说明及新增或修改的 Markdown 正文使用中文；代码、命令、路径、字段名和其他技术标识保持原样。
- 先给影响与结论，再给行动、待决策和必要证据；没有对应内容就省略。
- 使用具体、简单的词，避免无意义术语、套话、重复总结和未经请求的对比。
- 技术细节只保留对理解结论、判断风险或复现结果有帮助的部分。

## 执行方式

- 用户表示要开始新工作或修复现有问题时，持续推进，直到用户的目标完成，在目标方向上自主推进。
- 向用户提问之前，先完成上下文里已经授权、并且能把下一步变成可审查结果的工作。用户批准的应该是具体、可检查的结果。
- 用户建议不适合目标时直接说，不要迎合。
- 不要因为假想风险，主动加警告、免责声明、审批流程或安全、合规清单。

## 项目范围

- 主要目标是 AI 推理系统工程师面试，主体研究对象是 SGLang 的等待请求排序。
- 共同语言写入 `CONTEXT.md`；源码依据和策略比较写入调研文档，当前方案写入项目设计，执行顺序与阶段状态写入实现计划。
- 当前阶段沿用上游 KV 分配、引用管理和批次准入机制。扩展范围应由实际问题决定。
- 不允许在代码或实验流程中添加为了应对臆想风险、且不影响实验正确性的门禁、哈希校验、冒烟检查、契约等生产级程序化约束。

## 测试与验证

- 不要为可逆、影响小、只是复述实现的改动写测试。
- 调度实现只进行与请求顺序、等待语义、缓存复用和实验正确性直接相关的验证；不建立无关的生产验证体系。
- 本地 Windows 静态分析和模拟结果不能替代远程服务器的真实 GPU 结果。
- 记录实际运行条件和原始实验结果；未实现方案、预期收益和实测结果要明确区分。

## 文件流转

- 远程服务器无外网；依赖、模型和输入由 Linux 登录设备准备并传入。
- 服务器与 Linux 登录设备不向 GitHub 推送。产物回传后由操作者通过 Linux 登录设备上的 GitHub 网页端手动提交。
- GitHub 发布不是开始实验的前置条件。
